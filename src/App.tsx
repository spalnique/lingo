const App = () => {
  return (
    <p
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: window.innerHeight,
        textTransform: 'capitalize',
      }}>
      hello world
    </p>
  );
};

export default App;
